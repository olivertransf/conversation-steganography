# How it works site

Static walkthrough of the Conversation Stenography pipeline.

```sh
open index.html
# or
python3 -m http.server 8080
```

Then open `http://localhost:8080`.

Deploy the `website/` folder to Netlify or GitHub Pages as a static site root.
